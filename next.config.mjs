/** @type {import('next').NextConfig} */
const nextConfig = {
  // turn this on only if you need to bypass lint during builds
  // eslint: { ignoreDuringBuilds: true },

  // if you added any runtime tweaks, keep them here
  // experimental: {},
};

export default nextConfig;
